# Generated by `speclock plan` from the spec below. Re-running plan is safe:
# it preserves the `tests:` you add and only syncs ids/descriptions from the spec.
#
# A criterion is covered by any test whose name contains its id in brackets,
# e.g. [AC-1]. You can also list explicit test-name substrings under `tests:`.
version: 1
spec: SPEC.md
criteria:
  - id: SL-1
    description: "`speclock init` scaffolds a SPEC.md template"
    tests: []
  - id: SL-2
    description: speclock parses a SPEC.md into acceptance criteria
    tests: []
  - id: SL-3
    description: "`speclock plan` locks criteria into specs/*.yaml"
    tests: []
  - id: SL-4
    description: the resolver maps criteria to tests
    tests: []
  - id: SL-5
    description: "`speclock check` gates on the spec"
    tests: []
  - id: SL-6
    description: a Vitest adapter runs the suite
    tests: []
  - id: SL-7
    description: "`speclock status` prints a coverage map"
    tests: []
  - id: SL-8
    description: the core is pure
    tests: []
  - id: SL-9
    description: speclock only writes spec and lock files
    tests: []
